#include "main.h"

/**
 * mul -Function to print multiplication
 * @a: first variable
 * @b: second variables
 * Return: Always 0
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
