#include "main.h"

/** _isupper - Functio to print is integer is upper or not
 *
 * Return: returns 1 otherwise 0
 */
int _isupper(int c)
{
	for (c = 65; c <= 90; c++)
	{
		if (c = 97 || c <= 122)
		{
			return (0);
			continue;
		}
	}
	_putchar('\n');
	return (1);
}
