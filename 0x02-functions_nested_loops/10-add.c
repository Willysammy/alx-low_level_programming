#include "main.h"

/**
 * add - Function to add two ints and return the result
 *
 * @a, b: variables to declare
 * Return: Return value
 */
int add(int a, int b)
{
	int a;
	int b;
	int result = a + b;

	return (result);

	add(2, 5);
}
