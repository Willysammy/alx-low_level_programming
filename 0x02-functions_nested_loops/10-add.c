#include "main.h"

/**
 * add - Function to add two ints and return the result
 * @a: first variable
 * @b: second variable
 * Return: Return value
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
